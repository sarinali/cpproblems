#include "/Users/sarinali/Downloads/test/stdc++.h"
using namespace std;
// moneky word -> B + a word
// if more after s then it is not monkey word
// terminate after s
void check(string s) {
    if (s[0]=='B'|| s[0]=='A') {

    } 
}

int main() {
    cin.tie(0); cin.sync_with_stdio(0);
    return 0;
    while (true) {
        string s;
        cin >> s;
        if (s=='X') {
            break;
        }
    }
}